VITE_API_BASE_URL=http://localhost:8000

# Enable the mic button (requires backend STT + GCP Speech-to-Text API enabled).
VITE_ENABLE_VOICE=true

# Frontdesk mode auto-enables TTS playback and auto-sends voice transcripts.
VITE_FRONTDESK_MODE=true
