{
  "info": {
    "_postman_id": "3b030caa-3d85-59d5-a249-4c1fc684d500",
    "name": "HIS Integration (hospital-facing)",
    "description": "Only the calls **our system makes to the hospital HIS** — the integration surface for the hospital IT team.\n\n**imed-assignment** — what we intend to send to your real iMed `POST /patient-assignments`, plus the fields we are asking for. Start here.\n\n**our-current-mock** — what our adapter calls today against our own mock HIS. Everything in it that iMed does not document is marked ⚠️ **OUR ASSUMPTION**; the visit lookup is the blocking one.\n\nAuth: both schemes are sent from the single `hisToken` variable (`Authorization: Bearer` for iMed, `X-API-Key` for our mock) — matching `his_auth_headers()` in the backend. On the real API we will send Bearer only.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{hisToken}}",
        "type": "string"
      }
    ]
  },
  "item": [
    {
      "name": "imed-assignment",
      "description": "What we intend to send to `POST /patient-assignments`.\n\n**These are our payloads, not a transcription of your contract** — your\ncontract is transcribed verbatim in `docs/imed-patient-assignment-api.md` and\nwe have left it untouched. Every ⚠️ value is a placeholder awaiting your\nmaster data.\n\n1. **as we will send it** — the real payload. Open its **Examples** dropdown\n   to see how we handle each response, including every error code.\n2. **PROPOSED additions** — fields we are asking for, isolated in one\n   additive object so there is no doubt about what we send today.\n3. **PROPOSED lookup** — a read endpoint that would resolve the\n   post-timeout unknown state.\n",
      "item": [
        {
          "name": "1. POST /patient-assignments — as we will send it",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{imedBaseUrl}}/patient-assignments",
              "host": [
                "{{imedBaseUrl}}"
              ],
              "path": [
                "patient-assignments"
              ]
            },
            "description": "**WE POST** — this is *our intended payload*, not a transcription of your\ncontract. The contract itself is transcribed verbatim in\n`docs/imed-patient-assignment-api.md`.\n\n⚠️ `assign_spid` is a **placeholder** pending your master-data codes.\n\n### Deliberately absent\n\n| Field | Why |\n|---|---|\n| `queue_number` | you generate it — your queue rules own sequencing, prefixes and daily resets. We display what you return and the nurse hands it to the patient. |\n| `base_department_id` | you derive it from the service point; one less code for us to keep in sync |\n| `assign_eid` | we route to a *department*, never a named doctor. If any destination requires one, that is a product decision for us — see per-department question 2. |\n| `sbar.assessment_equipment` | a clinical judgement our system does not make; left for the nurse |\n\n### Where each value comes from\n\n| iMed field | our source |\n|---|---|\n| `request_id` | allocated at nurse-confirm and **stored**; reused on retry, new one on a genuine reroute |\n| `visit_id` | `session.metadata.visit.visit_id`, captured when the patient enters their VN at the booth |\n| `assign_spid` | our department code → `department_map.CODE_TO_SPID` ⚠️ |\n| `sbar.situation` | nurse-edited chief complaint, else `metadata.triage_classification.symptoms_summary` |\n| `sbar.background` | `metadata.patient_history` (chronic_conditions, allergies, past_surgeries, family_history, smoking_alcohol) + age |\n| `sbar.assessment` | `metadata.vitals` measured at the booth **+ the triage level** (it has nowhere else to go — see change request 1) |\n| `sbar.assessment_problem` | `triage_classification.key_reason` + `disposition_reasons[].text_th` with its manual `citation` |\n| `sbar.recommend` | destination department + urgency; a reroute is noted here because iMed has no `rerouted` flag |\n\nSBAR is always sent in **Thai**, regardless of the language the patient used\nat the booth.\n\n### Timeout is not failure\n\nPostman cannot express this as a saved example, so stated here: if this call\n**times out**, we do not know whether the queue row was created. We record it\nas `unknown` — never as failed — and any retry reuses the **same**\n`request_id`, so it cannot double-book the patient. This is why change\nrequests 7 and 8 matter.\n",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "response": [
            {
              "name": "200 — STATUS_SUCCESS",
              "originalRequest": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{imedBaseUrl}}/patient-assignments",
                  "host": [
                    "{{imedBaseUrl}}"
                  ],
                  "path": [
                    "patient-assignments"
                  ]
                },
                "description": "**WE POST** — this is *our intended payload*, not a transcription of your\ncontract. The contract itself is transcribed verbatim in\n`docs/imed-patient-assignment-api.md`.\n\n⚠️ `assign_spid` is a **placeholder** pending your master-data codes.\n\n### Deliberately absent\n\n| Field | Why |\n|---|---|\n| `queue_number` | you generate it — your queue rules own sequencing, prefixes and daily resets. We display what you return and the nurse hands it to the patient. |\n| `base_department_id` | you derive it from the service point; one less code for us to keep in sync |\n| `assign_eid` | we route to a *department*, never a named doctor. If any destination requires one, that is a product decision for us — see per-department question 2. |\n| `sbar.assessment_equipment` | a clinical judgement our system does not make; left for the nurse |\n\n### Where each value comes from\n\n| iMed field | our source |\n|---|---|\n| `request_id` | allocated at nurse-confirm and **stored**; reused on retry, new one on a genuine reroute |\n| `visit_id` | `session.metadata.visit.visit_id`, captured when the patient enters their VN at the booth |\n| `assign_spid` | our department code → `department_map.CODE_TO_SPID` ⚠️ |\n| `sbar.situation` | nurse-edited chief complaint, else `metadata.triage_classification.symptoms_summary` |\n| `sbar.background` | `metadata.patient_history` (chronic_conditions, allergies, past_surgeries, family_history, smoking_alcohol) + age |\n| `sbar.assessment` | `metadata.vitals` measured at the booth **+ the triage level** (it has nowhere else to go — see change request 1) |\n| `sbar.assessment_problem` | `triage_classification.key_reason` + `disposition_reasons[].text_th` with its manual `citation` |\n| `sbar.recommend` | destination department + urgency; a reroute is noted here because iMed has no `rerouted` flag |\n\nSBAR is always sent in **Thai**, regardless of the language the patient used\nat the booth.\n\n### Timeout is not failure\n\nPostman cannot express this as a saved example, so stated here: if this call\n**times out**, we do not know whether the queue row was created. We record it\nas `unknown` — never as failed — and any retry reuses the **same**\n`request_id`, so it cannot double-book the patient. This is why change\nrequests 7 and 8 matter.\n",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  }\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "status": "OK",
              "code": 200,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "cookie": [],
              "body": "// Nurse sees: the queue number, and gives it to the patient.\n{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"status\": \"STATUS_SUCCESS\",\n  \"result\": {\n    \"visit_id\": \"VN-2026-0001\",\n    \"visit_queue_id\": \"VQ-8F2C1A9D44\",\n    \"assign_spid\": \"SP_OPD_MED_01\",\n    \"assign_eid\": \"EMP00001\",\n    \"queue_number\": \"A014\",\n    \"queue_status\": \"WAITING\",\n    \"sbar_id\": \"SBAR-4B7E22\"\n  }\n}"
            },
            {
              "name": "409 — VISIT_QUEUE_ALREADY_EXIST",
              "originalRequest": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{imedBaseUrl}}/patient-assignments",
                  "host": [
                    "{{imedBaseUrl}}"
                  ],
                  "path": [
                    "patient-assignments"
                  ]
                },
                "description": "**WE POST** — this is *our intended payload*, not a transcription of your\ncontract. The contract itself is transcribed verbatim in\n`docs/imed-patient-assignment-api.md`.\n\n⚠️ `assign_spid` is a **placeholder** pending your master-data codes.\n\n### Deliberately absent\n\n| Field | Why |\n|---|---|\n| `queue_number` | you generate it — your queue rules own sequencing, prefixes and daily resets. We display what you return and the nurse hands it to the patient. |\n| `base_department_id` | you derive it from the service point; one less code for us to keep in sync |\n| `assign_eid` | we route to a *department*, never a named doctor. If any destination requires one, that is a product decision for us — see per-department question 2. |\n| `sbar.assessment_equipment` | a clinical judgement our system does not make; left for the nurse |\n\n### Where each value comes from\n\n| iMed field | our source |\n|---|---|\n| `request_id` | allocated at nurse-confirm and **stored**; reused on retry, new one on a genuine reroute |\n| `visit_id` | `session.metadata.visit.visit_id`, captured when the patient enters their VN at the booth |\n| `assign_spid` | our department code → `department_map.CODE_TO_SPID` ⚠️ |\n| `sbar.situation` | nurse-edited chief complaint, else `metadata.triage_classification.symptoms_summary` |\n| `sbar.background` | `metadata.patient_history` (chronic_conditions, allergies, past_surgeries, family_history, smoking_alcohol) + age |\n| `sbar.assessment` | `metadata.vitals` measured at the booth **+ the triage level** (it has nowhere else to go — see change request 1) |\n| `sbar.assessment_problem` | `triage_classification.key_reason` + `disposition_reasons[].text_th` with its manual `citation` |\n| `sbar.recommend` | destination department + urgency; a reroute is noted here because iMed has no `rerouted` flag |\n\nSBAR is always sent in **Thai**, regardless of the language the patient used\nat the booth.\n\n### Timeout is not failure\n\nPostman cannot express this as a saved example, so stated here: if this call\n**times out**, we do not know whether the queue row was created. We record it\nas `unknown` — never as failed — and any retry reuses the **same**\n`request_id`, so it cannot double-book the patient. This is why change\nrequests 7 and 8 matter.\n",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  }\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "status": "Conflict",
              "code": 409,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "cookie": [],
              "body": "// We treat this as SUCCESS (our earlier attempt landed). But with no `result` we never learn queue_number, so the nurse has nothing to hand over -> change request 7.\n{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"status\": \"STATUS_BUSINESS_ERROR\",\n  \"message\": \"VISIT_QUEUE_ALREADY_EXIST\",\n  \"message_th\": \"ผู้ป่วยอยู่ในคิวของจุดบริการปลายทางแล้ว\"\n}"
            },
            {
              "name": "403 — VISIT_LOCKED_OR_FINANCIAL_DISCHARGED",
              "originalRequest": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{imedBaseUrl}}/patient-assignments",
                  "host": [
                    "{{imedBaseUrl}}"
                  ],
                  "path": [
                    "patient-assignments"
                  ]
                },
                "description": "**WE POST** — this is *our intended payload*, not a transcription of your\ncontract. The contract itself is transcribed verbatim in\n`docs/imed-patient-assignment-api.md`.\n\n⚠️ `assign_spid` is a **placeholder** pending your master-data codes.\n\n### Deliberately absent\n\n| Field | Why |\n|---|---|\n| `queue_number` | you generate it — your queue rules own sequencing, prefixes and daily resets. We display what you return and the nurse hands it to the patient. |\n| `base_department_id` | you derive it from the service point; one less code for us to keep in sync |\n| `assign_eid` | we route to a *department*, never a named doctor. If any destination requires one, that is a product decision for us — see per-department question 2. |\n| `sbar.assessment_equipment` | a clinical judgement our system does not make; left for the nurse |\n\n### Where each value comes from\n\n| iMed field | our source |\n|---|---|\n| `request_id` | allocated at nurse-confirm and **stored**; reused on retry, new one on a genuine reroute |\n| `visit_id` | `session.metadata.visit.visit_id`, captured when the patient enters their VN at the booth |\n| `assign_spid` | our department code → `department_map.CODE_TO_SPID` ⚠️ |\n| `sbar.situation` | nurse-edited chief complaint, else `metadata.triage_classification.symptoms_summary` |\n| `sbar.background` | `metadata.patient_history` (chronic_conditions, allergies, past_surgeries, family_history, smoking_alcohol) + age |\n| `sbar.assessment` | `metadata.vitals` measured at the booth **+ the triage level** (it has nowhere else to go — see change request 1) |\n| `sbar.assessment_problem` | `triage_classification.key_reason` + `disposition_reasons[].text_th` with its manual `citation` |\n| `sbar.recommend` | destination department + urgency; a reroute is noted here because iMed has no `rerouted` flag |\n\nSBAR is always sent in **Thai**, regardless of the language the patient used\nat the booth.\n\n### Timeout is not failure\n\nPostman cannot express this as a saved example, so stated here: if this call\n**times out**, we do not know whether the queue row was created. We record it\nas `unknown` — never as failed — and any retry reuses the **same**\n`request_id`, so it cannot double-book the patient. This is why change\nrequests 7 and 8 matter.\n",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  }\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "status": "Forbidden",
              "code": 403,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "cookie": [],
              "body": "// Permanent — we do NOT retry. Nurse sees: visit is closed, send the patient to the front desk.\n{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"status\": \"STATUS_BUSINESS_ERROR\",\n  \"message\": \"VISIT_LOCKED_OR_FINANCIAL_DISCHARGED\",\n  \"message_th\": \"visit ถูกล็อกหรือจำหน่ายทางการเงินแล้ว\"\n}"
            },
            {
              "name": "422 — SERVICE_POINT_NOT_AVAILABLE",
              "originalRequest": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{imedBaseUrl}}/patient-assignments",
                  "host": [
                    "{{imedBaseUrl}}"
                  ],
                  "path": [
                    "patient-assignments"
                  ]
                },
                "description": "**WE POST** — this is *our intended payload*, not a transcription of your\ncontract. The contract itself is transcribed verbatim in\n`docs/imed-patient-assignment-api.md`.\n\n⚠️ `assign_spid` is a **placeholder** pending your master-data codes.\n\n### Deliberately absent\n\n| Field | Why |\n|---|---|\n| `queue_number` | you generate it — your queue rules own sequencing, prefixes and daily resets. We display what you return and the nurse hands it to the patient. |\n| `base_department_id` | you derive it from the service point; one less code for us to keep in sync |\n| `assign_eid` | we route to a *department*, never a named doctor. If any destination requires one, that is a product decision for us — see per-department question 2. |\n| `sbar.assessment_equipment` | a clinical judgement our system does not make; left for the nurse |\n\n### Where each value comes from\n\n| iMed field | our source |\n|---|---|\n| `request_id` | allocated at nurse-confirm and **stored**; reused on retry, new one on a genuine reroute |\n| `visit_id` | `session.metadata.visit.visit_id`, captured when the patient enters their VN at the booth |\n| `assign_spid` | our department code → `department_map.CODE_TO_SPID` ⚠️ |\n| `sbar.situation` | nurse-edited chief complaint, else `metadata.triage_classification.symptoms_summary` |\n| `sbar.background` | `metadata.patient_history` (chronic_conditions, allergies, past_surgeries, family_history, smoking_alcohol) + age |\n| `sbar.assessment` | `metadata.vitals` measured at the booth **+ the triage level** (it has nowhere else to go — see change request 1) |\n| `sbar.assessment_problem` | `triage_classification.key_reason` + `disposition_reasons[].text_th` with its manual `citation` |\n| `sbar.recommend` | destination department + urgency; a reroute is noted here because iMed has no `rerouted` flag |\n\nSBAR is always sent in **Thai**, regardless of the language the patient used\nat the booth.\n\n### Timeout is not failure\n\nPostman cannot express this as a saved example, so stated here: if this call\n**times out**, we do not know whether the queue row was created. We record it\nas `unknown` — never as failed — and any retry reuses the **same**\n`request_id`, so it cannot double-book the patient. This is why change\nrequests 7 and 8 matter.\n",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  }\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "status": "Unprocessable Entity",
              "code": 422,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "cookie": [],
              "body": "// Recoverable. We reopen the department choice so the nurse can reroute immediately.\n{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"status\": \"STATUS_BUSINESS_ERROR\",\n  \"message\": \"SERVICE_POINT_NOT_AVAILABLE\",\n  \"message_th\": \"จุดบริการปลายทางไม่พร้อมให้บริการ\"\n}"
            },
            {
              "name": "400 — invalid_request",
              "originalRequest": {
                "method": "POST",
                "header": [
                  {
                    "key": "Content-Type",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{imedBaseUrl}}/patient-assignments",
                  "host": [
                    "{{imedBaseUrl}}"
                  ],
                  "path": [
                    "patient-assignments"
                  ]
                },
                "description": "**WE POST** — this is *our intended payload*, not a transcription of your\ncontract. The contract itself is transcribed verbatim in\n`docs/imed-patient-assignment-api.md`.\n\n⚠️ `assign_spid` is a **placeholder** pending your master-data codes.\n\n### Deliberately absent\n\n| Field | Why |\n|---|---|\n| `queue_number` | you generate it — your queue rules own sequencing, prefixes and daily resets. We display what you return and the nurse hands it to the patient. |\n| `base_department_id` | you derive it from the service point; one less code for us to keep in sync |\n| `assign_eid` | we route to a *department*, never a named doctor. If any destination requires one, that is a product decision for us — see per-department question 2. |\n| `sbar.assessment_equipment` | a clinical judgement our system does not make; left for the nurse |\n\n### Where each value comes from\n\n| iMed field | our source |\n|---|---|\n| `request_id` | allocated at nurse-confirm and **stored**; reused on retry, new one on a genuine reroute |\n| `visit_id` | `session.metadata.visit.visit_id`, captured when the patient enters their VN at the booth |\n| `assign_spid` | our department code → `department_map.CODE_TO_SPID` ⚠️ |\n| `sbar.situation` | nurse-edited chief complaint, else `metadata.triage_classification.symptoms_summary` |\n| `sbar.background` | `metadata.patient_history` (chronic_conditions, allergies, past_surgeries, family_history, smoking_alcohol) + age |\n| `sbar.assessment` | `metadata.vitals` measured at the booth **+ the triage level** (it has nowhere else to go — see change request 1) |\n| `sbar.assessment_problem` | `triage_classification.key_reason` + `disposition_reasons[].text_th` with its manual `citation` |\n| `sbar.recommend` | destination department + urgency; a reroute is noted here because iMed has no `rerouted` flag |\n\nSBAR is always sent in **Thai**, regardless of the language the patient used\nat the booth.\n\n### Timeout is not failure\n\nPostman cannot express this as a saved example, so stated here: if this call\n**times out**, we do not know whether the queue row was created. We record it\nas `unknown` — never as failed — and any retry reuses the **same**\n`request_id`, so it cannot double-book the patient. This is why change\nrequests 7 and 8 matter.\n",
                "body": {
                  "mode": "raw",
                  "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  }\n}",
                  "options": {
                    "raw": {
                      "language": "json"
                    }
                  }
                }
              },
              "status": "Bad Request",
              "code": 400,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "cookie": [],
              "body": "// Our bug, not a nurse problem. Logged and alerted; nurse sees 'system error, contact IT'.\n{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"status\": \"STATUS_BUSINESS_ERROR\",\n  \"message\": \"invalid_request\",\n  \"message_th\": \"ข้อมูลไม่ครบหรือไม่ถูกต้อง\"\n}"
            }
          ]
        },
        {
          "name": "2. POST /patient-assignments — PROPOSED additions (NOT in your contract)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{imedBaseUrl}}/patient-assignments",
              "host": [
                "{{imedBaseUrl}}"
              ],
              "path": [
                "patient-assignments"
              ]
            },
            "description": "**Nothing here is in your contract yet — this is the ask.**\n\nIdentical to request 1, plus a single additive `proposed` object. It is\ndeliberately one wrapper rather than loose top-level fields, so there is no\nchance of reading it as something we already send. **We are not asking you to\nadopt this shape** — only to tell us where these values should live.\n\n| In `proposed` | Change request | Why it matters |\n|---|---|---|\n| `acuity_level`, `acuity_scale` | **CR 1 — highest value** | The 5-level triage is our system's whole output. With no field it is buried in SBAR prose, so your destination queue sorts by arrival time instead of by how sick the patient is. |\n| `vitals` (structured, with `measured_at` and `source`) | CR 2 | We measure with a real cuff at the booth and know whether a value was instrument-measured or patient-stated. Today all of it flattens into one sentence. |\n| `confirmed_by` | CR 3 | Sender identity comes from the token, so iMed records \"the MFU triage system\", not the nurse who signed off. Matters for audit after an incident. |\n| `source_ref` | CR 4 | Lets your staff open the full transcript and the AI's cited reasoning, not just the summary. |\n| `rerouted` | — | We know when a nurse overrode the AI's department. There is no iMed field, so today it can only go into `sbar.recommend` as prose. |\n\nIf any of these are accepted we implement them our side and this request\nbecomes request 1.\n\n*Against our mock this reuses request 1's `request_id`, so it replays request\n1's result rather than queueing the patient twice — that is the idempotency\nrule working, not the proposed fields being ignored.*\n",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"visit_id\": \"{{imedVisitId}}\",\n  \"assign_spid\": \"SP_OPD_MED_01\",\n  \"sbar\": {\n    \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n    \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n    \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n    \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n    \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n  },\n  \"proposed\": {\n    \"acuity_level\": 3,\n    \"acuity_scale\": \"MOPH-5\",\n    \"vitals\": {\n      \"systolic\": 158,\n      \"diastolic\": 94,\n      \"pulse_bpm\": 96,\n      \"temperature_c\": 36.8,\n      \"weight_kg\": 72.5,\n      \"height_cm\": 165,\n      \"measured_at\": \"2026-08-07T09:12:00+07:00\",\n      \"source\": \"cuff\"\n    },\n    \"confirmed_by\": \"OPD Nurse (สมหญิง)\",\n    \"source_ref\": {\n      \"slip_code\": \"MCH-A1B2-C3D4\",\n      \"session_ref\": \"{{session_id}}\"\n    },\n    \"rerouted\": false\n  }\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "3. GET /patient-assignments/{request_id} — PROPOSED lookup",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{imedBaseUrl}}/patient-assignments/{{imedRequestId}}",
              "host": [
                "{{imedBaseUrl}}"
              ],
              "path": [
                "patient-assignments",
                "{{imedRequestId}}"
              ]
            },
            "description": "**This endpoint does not exist yet — it is a request.** It covers three\nasks at once.\n\n**CR 8 — an idempotency key we cannot query leaves us blind.** If our call\ntimes out we cannot tell whether the assignment landed. Today the only way to\nfind out is to send it again and interpret the error. A read endpoint turns a\nguess into a fact, and enables end-of-day reconciliation against your gateway\nlogs.\n\n**CR 7 — a duplicate should return the original `result`.** Standard\nidempotency behaviour is 200 with the first response replayed. Without it, a\ntimeout-then-retry leaves the patient queued while the nurse has no queue\nnumber to give them.\n\n**CR 5 — SBAR read-back.** SBAR is currently write-only for us: we get an\n`sbar_id` and nothing else. Reading it back lets us show the nurse what was\nactually handed over.\n\n*Returns 404 against our mock — deliberately not built, because it is a\nrequest to you, not something we have decided for you.*\n"
          },
          "response": [
            {
              "name": "200 — assignment found (incl. SBAR read-back)",
              "originalRequest": {
                "method": "GET",
                "header": [],
                "url": {
                  "raw": "{{imedBaseUrl}}/patient-assignments/{{imedRequestId}}",
                  "host": [
                    "{{imedBaseUrl}}"
                  ],
                  "path": [
                    "patient-assignments",
                    "{{imedRequestId}}"
                  ]
                }
              },
              "status": "OK",
              "code": 200,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "cookie": [],
              "body": "// Resolves the post-timeout unknown, and returns what we handed over.\n{\n  \"request_id\": \"MFU-20260807-A1B2C3\",\n  \"status\": \"STATUS_SUCCESS\",\n  \"result\": {\n    \"visit_id\": \"VN-2026-0001\",\n    \"visit_queue_id\": \"VQ-8F2C1A9D44\",\n    \"assign_spid\": \"SP_OPD_MED_01\",\n    \"assign_eid\": \"EMP00001\",\n    \"queue_number\": \"A014\",\n    \"queue_status\": \"WAITING\",\n    \"sbar_id\": \"SBAR-4B7E22\",\n    \"sbar\": {\n      \"situation\": \"แน่นหน้าอกมา 2 ชั่วโมง ร้าวไปแขนซ้าย\",\n      \"background\": \"ความดันโลหิตสูง กินยาสม่ำเสมอ ไม่มีประวัติแพ้ยา อายุ 58 ปี\",\n      \"assessment\": \"BP 158/94, ชีพจร 96, อุณหภูมิ 36.8, SpO2 97% (วัดที่บูธ) — ระดับการคัดกรอง 3\",\n      \"assessment_problem\": \"เจ็บหน้าอกร่วมกับปัจจัยเสี่ยงโรคหัวใจ (คู่มือคัดกรอง MFU ข้อ 4.2)\",\n      \"recommend\": \"ส่งตรวจอายุรกรรม ประเมินโดยแพทย์\"\n    }\n  }\n}"
            }
          ]
        }
      ]
    },
    {
      "name": "our-current-mock",
      "description": "What our adapter calls **today**, against our own mock HIS.\n\nYour assignment contract covers one direction only, so everything here that\niMed does not document is marked ⚠️ **OUR ASSUMPTION (change request 6)**.\n\nThe **visit lookup** is blocking: the booth starts from a patient entering\ntheir VN, and without a real equivalent we cannot identify them, cannot pull\nhistory, and have no `visit_id` to assign later.\n",
      "item": [
        {
          "name": "GET /api/visits/{visit_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-API-Key",
                "value": "{{hisToken}}"
              }
            ],
            "url": {
              "raw": "{{hisBaseUrl}}/api/visits/{{visit_id}}",
              "host": [
                "{{hisBaseUrl}}"
              ],
              "path": [
                "api",
                "visits",
                "{{visit_id}}"
              ]
            },
            "description": "⚠️ **OUR ASSUMPTION — iMed documents no counterpart (change request 6).**\n\n**WE GET** — visit lookup: validate a visit id and pull patient name, birthdate, vitals, history (`HttpHisAdapter.validate_visit`). **Blocking for go-live**: without a real equivalent the booth cannot link a patient to a visit, and there is then no `visit_id` to assign.\n\nGet Visit"
          }
        },
        {
          "name": "GET /api/departments",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-API-Key",
                "value": "{{hisToken}}"
              }
            ],
            "url": {
              "raw": "{{hisBaseUrl}}/api/departments",
              "host": [
                "{{hisBaseUrl}}"
              ],
              "path": [
                "api",
                "departments"
              ]
            },
            "description": "⚠️ **OUR ASSUMPTION — iMed documents no counterpart (change request 6).**\n\n**WE GET** — list of department names for routing (`HttpHisAdapter.get_departments`). Replaceable by a one-time master-data sheet instead of an API.\n\nList Departments"
          }
        },
        {
          "name": "POST /api/visits/{visit_id}/prescreen",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-API-Key",
                "value": "{{hisToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{hisBaseUrl}}/api/visits/{{visit_id}}/prescreen",
              "host": [
                "{{hisBaseUrl}}"
              ],
              "path": [
                "api",
                "visits",
                "{{visit_id}}",
                "prescreen"
              ]
            },
            "description": "**WE POST** — Stage 1 write-back: AI prescreen result right after the booth session (`HttpHisAdapter.push_referral`).\n\nStage 1 write-back — objective booth data only.\n\nWrites the measurements taken at our booth + the booth as\nfirst_location/measure onto the visit row. The clinical narrative\n(complaint summary, reason) and the recommended department are HELD\nin prescreen_results (pending) and NOT published to the visit row\nuntil a nurse confirms at the destination (Stage 2).",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"session_ref\": \"string\",\n  \"slip_code\": \"string\",\n  \"recommended_department\": \"string\",\n  \"complaint\": \"string\",\n  \"reason\": \"string\",\n  \"vitals\": {},\n  \"reasons\": [\n    \"string\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "PUT /api/visits/{visit_id}/follow-up",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-API-Key",
                "value": "{{hisToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{hisBaseUrl}}/api/visits/{{visit_id}}/follow-up",
              "host": [
                "{{hisBaseUrl}}"
              ],
              "path": [
                "api",
                "visits",
                "{{visit_id}}",
                "follow-up"
              ]
            },
            "description": "⚠️ **OUR ASSUMPTION — iMed documents no counterpart (change request 6).**\n\n**WE PUT** — follow-up note captured during the interview (`HttpHisAdapter.push_follow_up`).\n\nRecord the patient's own follow-up question/concern from the booth.\n\nWritten as soon as the patient states it (end of the booth flow) —\nunlike the nurse narrative it needs no human sign-off, it IS the\npatient's verbatim words for the doctor.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"follow_up\": \"string\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        },
        {
          "name": "PUT /api/patients/{hn}/history",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-API-Key",
                "value": "{{hisToken}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{hisBaseUrl}}/api/patients/{{hn}}/history",
              "host": [
                "{{hisBaseUrl}}"
              ],
              "path": [
                "api",
                "patients",
                "{{hn}}",
                "history"
              ]
            },
            "description": "⚠️ **OUR ASSUMPTION — iMed documents no counterpart (change request 6).**\n\n**WE PUT** — patient history (allergies, chronic conditions, …) updated at the booth (`HttpHisAdapter.push_patient_history`).\n\nRecord first-time-patient history collected at the booth.\n\nUpserts the full history (no partial-field merge, matching this\nservice's other write-back endpoints) and stamps\n``history_recorded_at`` — from this point on the patient is no\nlonger first-time, on this visit and any future one.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"smoking_alcohol\": \"string\",\n  \"allergies\": \"string\",\n  \"chronic_conditions\": \"string\",\n  \"past_surgeries\": \"string\",\n  \"family_history\": \"string\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          }
        }
      ]
    }
  ]
}
