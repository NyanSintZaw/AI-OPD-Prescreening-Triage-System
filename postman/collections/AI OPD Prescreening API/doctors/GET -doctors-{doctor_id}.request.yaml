$kind: http-request
name: GET /doctors/{doctor_id}
description: |-
  Get a doctor with their full weekly schedule.

  **Auth:** none (patient-facing).
url: "{{baseUrl}}/doctors/{{doctor_id}}"
method: GET
auth:
  type: noauth
order: 3000
