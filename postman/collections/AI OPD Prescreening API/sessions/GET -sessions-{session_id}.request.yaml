$kind: http-request
name: GET /sessions/{session_id}
description: |-
  Get Session

  **Auth:** none (patient-facing).
url: "{{baseUrl}}/sessions/{{session_id}}"
method: GET
auth:
  type: noauth
order: 7000
