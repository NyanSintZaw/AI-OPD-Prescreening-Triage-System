$kind: http-request
name: GET /sessions/{session_id}/emergency-events
description: |-
  List Emergency Events

  **Auth:** none (patient-facing).
url: "{{baseUrl}}/sessions/{{session_id}}/emergency-events"
method: GET
auth:
  type: noauth
order: 21000
