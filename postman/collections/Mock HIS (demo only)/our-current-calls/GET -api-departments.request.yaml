$kind: http-request
name: GET /api/departments
description: |-
  ⚠️ **OUR ASSUMPTION — iMed documents no counterpart (change request 6).**

  **WE GET** — list of department names for routing (`HttpHisAdapter.get_departments`). Replaceable by a one-time master-data sheet instead of an API.

  List Departments
url: "{{hisBaseUrl}}/api/departments"
method: GET
headers:
  X-API-Key: "{{hisToken}}"
order: 2000
