---
category: Kiosk
---
MALI's presence on the kiosk and the only thing that moves there. States: `idle` (slow breathe), `listening` (ring expands with `level` 0–1), `thinking` (orbit spins), `speaking` (halo pulses with `level`). Default 160px; 240–320px on the idle screen.
