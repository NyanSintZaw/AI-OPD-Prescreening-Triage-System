---
category: Display
---
Status pill. `tone` for workflow states (success, info, warning, danger, accent). `level={1–5}` renders the MOPH triage level in its clinical colour — **nurse/admin surfaces only, never on the kiosk**. `dot` collapses to a 10px marker for dense tables.
