---
category: Display
---
A hairline container, 14px radius. `elevation="raised"` adds a soft ink-tinted shadow — use for at most one card per view. `title` + `aside` give a heading row (put a Badge in aside). Never nest Cards; never make a grid of identical cards the page structure.
