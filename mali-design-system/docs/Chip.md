---
category: Actions
---
A selectable pill for symptom tags and quick answers. `selected` inverts to ink. `onRemove` adds a ×. Use `size="kiosk"` (56px) for patient quick-answers such as ใช่ / ไม่ใช่ / มากกว่า 3 วัน.
