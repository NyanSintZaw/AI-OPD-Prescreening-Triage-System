---
category: Feedback
---
Centred dialog, enters scale(.96)→1 over 300ms, Escape closes. Use only when the task needs protected focus (escalation confirm, destructive actions). Put a secondary + primary Button in `actions`.
