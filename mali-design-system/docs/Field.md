---
category: Forms
---
Label above, control, then hint or error. Errors must name the problem and the fix ("Enter 10 digits, e.g. 0812345678"). Pass `htmlFor` matching the control id. `required` adds a red asterisk.
