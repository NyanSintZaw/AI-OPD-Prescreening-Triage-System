---
category: Display
---
Dense portal table: 11px tracked uppercase headers, tabular numerals, hairline rows. `density="compact"` for the nurse review queue. The triage Badge inside it should be the only colour on the page. Provide `empty` copy in the product's voice.
