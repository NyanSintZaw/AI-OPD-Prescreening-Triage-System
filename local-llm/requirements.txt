# Ollama relay only — no torch, no CUDA, no ML stack. That is the point:
# this runs anywhere, including a machine with no GPU, because all the
# inference happens in Ollama on the other side of loopback.
fastapi
uvicorn[standard]
httpx
python-dotenv
