meta {
  name: GET /admin/criteria/versions/{version_id}
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/criteria/versions/{{version_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

docs {
  Get Criteria Version Detail
  
  Roles: super_admin, admin, viewer.
}
