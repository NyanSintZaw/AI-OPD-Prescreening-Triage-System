meta {
  name: POST /admin/criteria/versions/{version_id}/submit
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/admin/criteria/versions/{{version_id}}/submit
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

docs {
  Submit Criteria Version
  
  Roles: super_admin, nurse.
}
