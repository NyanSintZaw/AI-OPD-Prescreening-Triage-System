meta {
  name: POST /admin/criteria/versions/{version_id}/approve
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/admin/criteria/versions/{{version_id}}/approve
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

docs {
  Approve Criteria Version
  
  Roles: super_admin, nurse.
}
