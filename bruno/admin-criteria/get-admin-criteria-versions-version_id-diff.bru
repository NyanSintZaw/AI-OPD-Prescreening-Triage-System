meta {
  name: GET /admin/criteria/versions/{version_id}/diff
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/admin/criteria/versions/{{version_id}}/diff?against=
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

docs {
  Section-level diff (added/removed/changed rule ids) vs another version.
  
  Roles: super_admin, admin, viewer.
}
