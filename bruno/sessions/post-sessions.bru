meta {
  name: POST /sessions
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/sessions
  body: json
  auth: none
}

body:json {
  {
    "language": "th",
    "user_agent": "string",
    "ip_hash": "string",
    "metadata": {}
  }
}

script:post-response {
  bru.setEnvVar("session_id", res.body.id);
}

docs {
  Create Session
}
