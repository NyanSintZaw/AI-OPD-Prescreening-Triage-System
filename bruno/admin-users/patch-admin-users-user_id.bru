meta {
  name: PATCH /admin/users/{user_id}
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/admin/users/{{user_id}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "full_name": "string",
    "password": "string",
    "is_active": false
  }
}

docs {
  Admin Update User
  
  Roles: super_admin.
}
